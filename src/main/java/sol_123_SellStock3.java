public class sol_123_SellStock3 {
    public int maxProfit(int[] prices) {
        //Arrays of mins and maxs
        // left to right , find maxprofit for each max as end;
        // right to left, find maxprofit for each min as start;
        // Return max(l2r[i] + r2l[i + 1])
        return 0;
    }
}
